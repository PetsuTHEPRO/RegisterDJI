# 🎯 Fluxos de Operação - DroneMissionManager

## 1. Fluxo de Upload de Missão

```
┌─────────────────────────────────────────────────────────────────────┐
│ prepareAndUploadMission(missionData)                                │
│ suspend fun                                                         │
└─────────────────────────────────────────────────────────────────────┘
                                ↓
                    ┌───────────────────────┐
                    │ PREPARING (state)     │
                    └───────────────────────┘
                                ↓
              ┌─────────────────────────────────────┐
              │ 1. validateAndFilterWaypoints()      │
              │    - Verifica lista vazia           │
              │    - Filtra por altitude (min/max)  │
              └─────────────────────────────────────┘
                                ↓
             ┌──────────────────────────────────┐
             │ 2. validateFlightParameters()     │
             │    - Auto speed: 0.5-20 m/s      │
             │    - Max speed: 0.5-30 m/s       │
             │    - Max >= Auto                 │
             └──────────────────────────────────┘
                                ↓
             ┌──────────────────────────────────┐
             │ 3. buildWaypointMission()         │
             │    - Construir objeto mission     │
             │    - Setar todos os parâmetros   │
             └──────────────────────────────────┘
                                ↓
             ┌──────────────────────────────────┐
             │ 4. loadMission(mission)          │
             │    - Carregar para SDK           │
             │    - Verificar erro              │
             └──────────────────────────────────┘
                                ↓
                    ┌───────────────────────┐
                    │ UPLOADING (state)     │
                    └───────────────────────┘
                                ↓
         ┌──────────────────────────────────────────┐
         │ 5. withTimeout(30s) {                    │
         │      uploadMissionSuspend(operator)      │
         │      ↓                                    │
         │      Suspender até callback return       │
         │    }                                     │
         └──────────────────────────────────────────┘
                                ↓
                    ┌───────────────────────┐
                    │ READY_TO_EXECUTE      │
                    │ (state)               │
                    └───────────────────────┘
                                ↓
                    ✅ SUCCESS - Pronto para start
```

---

## 2. Fluxo de Execução de Missão

```
┌────────────────────────────────────────┐
│ startMission()                         │
│ suspend fun                            │
└────────────────────────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Verifica state == READY_TO_EXECUTE │
   │ Se não: throw DJIMissionException  │
   └────────────────────────────────────┘
                ↓
    ┌──────────────────────────────────┐
    │ withTimeout(10s) {               │
    │   startMissionSuspend(operator)  │
    │ }                                │
    └──────────────────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ Listener recebe onExecutionStart() │
   │ state = EXECUTING                  │
   └────────────────────────────────────┘
                ↓
    ┌──────────────────────────────────┐
    │ Durante execução:                │
    │ onExecutionUpdate() → state atua │
    │ onExecutionFinish() → state =    │
    │   FINISHED ou ERROR              │
    └──────────────────────────────────┘
```

---

## 3. Fluxo de Pausa/Retomada

```
            ┌──────────────────┐
            │ EXECUTING        │
            └──────────────────┘
                 ↙         ↘
        pauseMission()      resumeMission()
                 ↓              ↓
            ┌──────────┐    ┌──────────┐
            │ PAUSED   │    │ EXECUTING│
            └──────────┘    └──────────┘
```

---

## 4. Fluxo de Parada

```
┌──────────────────────────────────────┐
│ stopMission()                        │
│ suspend fun                          │
└──────────────────────────────────────┘
                ↓
       ┌─────────────────────┐
       │ withTimeout(10s) {  │
       │   stopMissionSuspend│
       │ }                   │
       └─────────────────────┘
                ↓
   ┌────────────────────────────────────┐
   │ state = EXECUTION_STOPPED          │
   └────────────────────────────────────┘
```

---

## 5. Fluxo de Lifecycle com Cleanup

```
┌────────────────────────────────────────────┐
│ Activity/Fragment Created                  │
│ viewModel.missionManager = DroneMissionMgr │
└────────────────────────────────────────────┘
                ↓
        ┌───────────────────┐
        │ init {} é chamado │
        │ - Monitoring      │
        │ - Listener add    │
        └───────────────────┘
                ↓
     ┌──────────────────────────┐
     │ User operations          │
     │ - upload/start/stop      │
     └──────────────────────────┘
                ↓
┌────────────────────────────────────────────┐
│ onDestroy() / ViewModel.onCleared()        │
│ missionManager.destroy() DEVE SER CHAMADO  │
└────────────────────────────────────────────┘
                ↓
        ┌───────────────────────┐
        │ destroy() {           │
        │ - Remove listener ✅  │
        │ - Para se executando  │
        │ - Log cleanup         │
        │ }                     │
        └───────────────────────┘
```

---

## 6. Fluxo de Tratamento de Erros

```
┌──────────────────────────────────┐
│ prepareAndUploadMission()        │
└──────────────────────────────────┘
        ↓
    ┌───────────────────┐
    │ Try block         │
    └───────────────────┘
        ↓
    ┌──────────────────────────────────┐
    │ Validações falham?               │
    │ ↓                                │
    │ throw IllegalArgumentException   │
    │ - Falta waypoints                │
    │ - Velocidade inválida            │
    │ - Altitude fora do range         │
    └──────────────────────────────────┘
        ↓ (catch)
    ┌──────────────────────────────────┐
    │ catch (e: IllegalArgumentException)
    │ state = ERROR                    │
    │ throw e                          │
    └──────────────────────────────────┘


    ┌──────────────────────────────────┐
    │ SDK operations falham?           │
    │ ↓                                │
    │ throw DJIMissionException        │
    │ - Erro ao carregar              │
    │ - Upload timeout                │
    │ - Erro no callback SDK          │
    └──────────────────────────────────┘
        ↓ (catch)
    ┌──────────────────────────────────┐
    │ catch (e: DJIMissionException)    │
    │ state = ERROR                    │
    │ throw e                          │
    │ (Caller faz Toast)              │
    └──────────────────────────────────┘
```

---

## 7. Timeline de Operação Típica

```
TEMPO     │ ESTADO              │ AÇÃO
──────────┼─────────────────────┼──────────────────────────
0ms       │ IDLE                │ Drone conectado
          │                     │
100ms     │ IDLE                │ Usuário clica "Upload"
          │                     │
110ms     │ PREPARING           │ Validando waypoints...
          │                     │
150ms     │ PREPARING           │ Carregando missão...
          │                     │
200ms     │ UPLOADING           │ Upload iniciado
          │                     │
2000ms    │ UPLOADING           │ Upload 50%
          │                     │
5000ms    │ UPLOADING           │ Upload 100%
          │                     │
5100ms    │ READY_TO_EXECUTE    │ Pronto para iniciar!
          │                     │
5200ms    │ READY_TO_EXECUTE    │ Usuário clica "Start"
          │                     │
5300ms    │ EXECUTING           │ Decolando...
          │                     │
15000ms   │ EXECUTING           │ Voando para waypoint 1/3
          │                     │
45000ms   │ EXECUTING           │ Voando para waypoint 2/3
          │                     │
75000ms   │ EXECUTING           │ Voando para waypoint 3/3
          │                     │
100000ms  │ EXECUTING           │ Retornando para casa
          │                     │
110000ms  │ EXECUTING           │ Pousando...
          │                     │
115000ms  │ FINISHED            │ Missão concluída! ✅
```

---

## 8. Estados Possíveis

```
┌────────────────────────────────────────────────────────┐
│                    IDLE                                │
│              (Drone conectado/inicial)                 │
└────────────────────────────────────────────────────────┘
                         ↓
┌────────────────────────────────────────────────────────┐
│                  PREPARING                             │
│         (Validando parâmetros/waypoints)               │
└────────────────────────────────────────────────────────┘
                    ↙        ↘
          ✅ válido        ❌ inválido
              ↓                ↓
         UPLOADING         ERROR ↔ IDLE
              ↓                ↑
         READY_TO_EXECUTE ←────┘
              ↓
          EXECUTING ←──── EXECUTION_PAUSED
              ↓                ↑
         FINISHED           EXECUTION_STOPPED
              ↓
           IDLE
```

---

## 9. Sequência de Chamadas (Sucesso)

```kotlin
// Ordem de execução esperada:

1️⃣  missionManager.prepareAndUploadMission(data)  // Suspend
2️⃣    → validateAndFilterWaypoints()              // Check altitude
3️⃣    → validateFlightParameters()                // Check speeds
4️⃣    → buildWaypointMission()                    // Create mission
5️⃣    → operator.loadMission()                    // Callback imediato
6️⃣    → uploadMissionSuspend()                    // Suspend até callback
7️⃣    → listener.onUploadUpdate()                 // Async callback
8️⃣    → listener.onUploadUpdate()                 // READY_TO_EXECUTE
9️⃣    ✅ Resume da suspend function

🔟  missionManager.startMission()                 // Suspend
1️⃣1️⃣   → startMissionSuspend()                   // Suspend até callback
1️⃣2️⃣   → listener.onExecutionStart()             // Async callback
1️⃣3️⃣   ✅ Resume da suspend function

1️⃣4️⃣  listener.onExecutionUpdate()               // Periódico
1️⃣5️⃣  listener.onExecutionFinish()               // Final
1️⃣6️⃣  missionManager.destroy()                   // Cleanup
```

---

## 10. Matriz de Transições de Estado

|      De      | Para | Condição | Sucesso? |
|:────────────:|:────:|:--------:|:--------:|
|    IDLE      | PREPARING | Usuário upload | ✅ |
| PREPARING    | UPLOADING | Validação OK | ✅ |
| PREPARING    | ERROR | Validação falhou | ❌ |
| UPLOADING    | READY_TO_EXECUTE | Upload OK | ✅ |
| UPLOADING    | ERROR | Upload falhou | ❌ |
| READY_TO_EXECUTE | EXECUTING | startMission() | ✅ |
| EXECUTING    | EXECUTION_PAUSED | pauseMission() | ✅ |
| EXECUTION_PAUSED | EXECUTING | resumeMission() | ✅ |
| EXECUTING    | EXECUTION_STOPPED | stopMission() | ✅ |
| EXECUTING    | FINISHED | Missão terminou | ✅ |
| Qualquer     | ERROR | Erro SDK/timeout | ❌ |
| ERROR        | IDLE | Reconexão | ✅ |

